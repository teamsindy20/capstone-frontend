query MenuDetail($id: ID!) {
  menu(id: $id) {
    ...menuCard
    content
    menuOptions {
      id
      name
      price
    }
    store {
      id
      name
      minimumDeliveryAmount
    }
  }
}
